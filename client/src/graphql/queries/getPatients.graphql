query getPatients($doctorId: Int!, $limit: Int) {
  getPatients(doctorId: $doctorId, limit: $limit) {
    id
    mrn
    doctorId
    updatedAt
    updatedAtString
    notes
    scans {
      id
    }
  }
}
