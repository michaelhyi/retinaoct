query getPatientScans($patientId: Int!, $limit: Int) {
  getPatientScans(patientId: $patientId, limit: $limit) {
    id
    uri
    updatedAtString
    diagnosis
    patient {
      id
      mrn
    }
  }
}
